# [di]gital[oc]ean, so dioc for short.
# implementing a client API library using a cURL wrapper, to fully understand what is being done
# whilst interacting with the Web API.


const curl = new (require('curl-request'))();

