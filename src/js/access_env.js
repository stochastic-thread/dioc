const process = require('process');
const cURL = require('curl-request');

const auth_token = process.env.DO_TOKEN_CURRENT;

# There are different Objects that you can interact with via the Web API.
# One such object is ACCOUNT.
#
# `account` example
# curl -X $HTTP_METHOD -u "$(auth_token)" "https://api.digitalocean.com/v2/account"





